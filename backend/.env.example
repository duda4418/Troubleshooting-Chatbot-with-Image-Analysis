# Example env file

OPENAI_API_KEY=
OPENAI_RESPONSE_MODEL=gpt-5-mini # Add in Azure environment variables
OPENAI_VISION_MODEL=gpt-5-nano # Add in Azure environment variables
# Pricing rates expressed per 1M tokens for input/output (JSON object) 
OPENAI_PRICING={"gpt-5-nano":{"input":0.05,"output":0.4},"gpt-5-mini":{"input":0.25,"output":2.0}} # Add in Azure environment variables

# App settings
APP_ENV=development # Add in Azure environment variables
SECRET_KEY=change_me_to_a_secure_random_value # Add in Azure environment variables

# Database (local docker defaults)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=ChangeMe!123
POSTGRES_DB=appdb
POSTGRES_HOST=db
POSTGRES_PORT=5432

# DATABASE_URL overrides the individual POSTGRES_* values (recommended for cloud providers).
# Example for Azure Flexible Server:
# DATABASE_URL=postgresql+asyncpg://postgres:ChangeMe!123@<HOST>:5432/appdb
DATABASE_URL=postgresql+asyncpg://postgres:ChangeMe!123@db:5432/appdb # Add in Azure environment variables

# Safe timeouts (ms) during migrations
DB_LOCK_TIMEOUT_MS=5000
DB_STATEMENT_TIMEOUT_MS=60000

# Auth
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS (comma separated)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
